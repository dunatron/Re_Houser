/// <reference types="cypress" />
import 'cypress-graphql-mock';

context('Login', () => {
  before(() => {
    // cy.deleteAccount();
    // maybe before just logout since we will be logged in from signing up
  });
  it('Should login', () => {
    // cy.login();
  });
});
