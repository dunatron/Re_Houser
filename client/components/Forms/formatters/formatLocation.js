import React from 'react';
import { is } from 'ramda';

const _preFormatLocation = value => {
  return value;
};

const _postFormatLocation = value => {
  return value;
};

export { _preFormatLocation, _postFormatLocation };
