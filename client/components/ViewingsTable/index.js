const ViewingsTable = ({ where }) => {
  return (
    <div>
      I am the viewings table component. I will simply take in a where
      statement. That way we can define in a HOC what the viewings returned
      should be.
    </div>
  );
};

export default ViewingsTable;
