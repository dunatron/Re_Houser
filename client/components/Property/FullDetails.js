/**
 * Will take an id as the param and will get all property details and display them
 */
const PropertyFullDetails = ({ id }) => {
  return <div>Property Full Details</div>;
};

export default PropertyFullDetails;
